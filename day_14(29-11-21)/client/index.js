document.addEventListener('DOMContentLoaded',()=>{
    let tdata = document.querySelector("tbody")
    // if (data.length === 0) {
    //     tdata.innerHTML = "No Data"
    // }
    tdata.innerHTML = "No Data"

})